var wins = 0;
var losses = 0;
var ties = 0;

var userChoice = prompt("please choose R,P,S");
var options = ["R", "P", "S"];
var computerChoice = options[Math.floor(Math.random() * types.length)];
var computerChoice = alert;

console.log(userChoice);
